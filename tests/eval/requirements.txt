# CRM-sale-Agent 量化评估依赖

# 核心依赖
pytest>=7.0.0
pytest-asyncio>=0.20.0
pydantic>=2.0.0
loguru>=0.7.0

# 评估辅助依赖
numpy>=1.24.0
matplotlib>=3.7.0

# Agent 核心依赖（用于真实模式评估）
llama-index>=0.8.0
chromadb>=0.4.0

# 可选依赖
# prometheus-client>=0.16.0  # Prometheus 指标收集
# jinja2>=3.1.0  # PDF 报告生成
