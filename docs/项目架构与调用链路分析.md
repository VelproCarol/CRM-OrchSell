# 可编排多工具销售任务拆解 Agent - 项目架构与调用链路分析

## 一、项目目录结构

```
sale-CRMAgent/
├── main.py                          # 服务入口
├── .env                             # 环境变量配置
├── requirements.txt                 # 依赖清单
├── config/                          # 配置层
│   ├── settings.py                  # 全局配置与常量定义
│   └── __init__.py
├── api/                             # API 层
│   ├── chat_route.py                # FastAPI 路由定义
│   └── __init__.py
├── core/                            # 核心层
│   ├── sales_agent.py               # Agent 主类（流程编排）
│   ├── task_planner.py              # 任务拆解器（LLM驱动）
│   ├── tool_dispatcher.py           # 工具调度器（串并行编排）
│   ├── reflection_engine.py         # 反思验真引擎
│   ├── llm_adapter.py               # 大模型适配器（OpenAI/Qwen）
│   ├── output_schema.py             # Pydantic 输出模型定义
│   └── __init__.py
├── tools/                           # 工具层
│   ├── base_tool.py                 # 工具抽象基类
│   ├── calculator_tool.py           # 计算器工具
│   ├── api_inventory_tool.py        # 库存查询工具（模拟CRM）
│   ├── sql_price_tool.py            # 价格查询工具（SQLite）
│   ├── doc_retrieve_tool.py         # 文档检索工具（Chroma）
│   └── __init__.py
├── storage/                         # 存储层
│   ├── init_sql.py                  # SQLite 初始化脚本
│   ├── init_vector_db.py            # Chroma 向量库初始化脚本
│   ├── sqlite/                      # SQLite 数据库文件
│   └── chroma_db/                   # Chroma 向量库文件
├── tests/                           # 测试层
│   ├── test_agent_flow.py           # Agent 流程测试
│   ├── test_api.py                  # API 接口测试
│   ├── test_tools.py                # 工具单元测试
│   └── test_reflection.py           # 反思验真测试
└── docs/                            # 业务文档目录
```

---

## 二、模块调用链路总览

### 2.1 服务启动初始化链路

```
main.py
  ├── setup_logging()                # 配置日志系统（loguru）
  │
  ├── initialize_system()            # 系统初始化
  │   ├── storage/init_sql.py        # 初始化 SQLite 数据库
  │   │   ├── 创建 products 表（产品信息）
  │   │   ├── 创建 deal_records 表（历史成交订单）
  │   │   ├── 创建 customers 表（客户档案）
  │   │   └── 插入模拟数据（8个产品）
  │   │
  │   └── storage/init_vector_db.py  # 初始化 Chroma 向量库
  │       ├── 加载本地 BGE Embedding 模型
  │       ├── 读取 docs/ 目录文档（.md/.txt/.json）
 │       └── 生成向量索引（5个模拟案例）
  │
  ├── create_app()                   # 创建 FastAPI 应用
  │   ├── api/chat_route.py
  │   │   ├── 注册 APIRouter（路由前缀 /api）
  │   │   ├── 配置 CORS 中间件
  │   │   └── 注册全局异常处理器
  │   └── 返回 FastAPI 实例
  │
  └── uvicorn.run()                  # 启动 ASGI 服务器
```

### 2.2 API 请求处理链路

```
HTTP Request
  └── api/chat_route.py
      ├── POST /api/chat/sales      # 销售咨询接口（主入口）
      │   ├── get_agent()           # 获取/初始化 SalesAgent 单例
      │   │   └── SalesAgent.__init__()
      │   │       ├── get_llm()     # 获取 LLMAdapter 单例
      │   │       ├── TaskPlanner() # 任务拆解器
      │   │       ├── ToolDispatcher() # 工具调度器
      │   │       └── ReflectionEngine() # 反思验真引擎
      │   │
      │   └── agent.process()       # Agent 主处理流程
      │       ├── Step 1: task_planner.plan()   # 任务拆解
      │       ├── Step 2: tool_dispatcher.dispatch() # 工具调度
      │       ├── Step 3: _build_response()      # 构建响应
      │       ├── Step 4: reflection_engine.verify() # 反思验真
      │       └── Step 5: 组装 TaskLog 返回
      │
      ├── GET /api/health           # 健康检查接口
      ├── GET /api/stats            # 统计信息接口
      ├── GET /api/tools            # 工具列表接口
      └── POST /api/init            # 数据库初始化接口
```

### 2.3 Agent 内部处理链路（核心）

```
SalesAgent.process(query, customer_id, product_category)
  │
  ├── Step 1: 任务拆解
  │   └── TaskPlanner.plan()
  │       └── llm_adapter.chat()   # 调用大模型拆解任务
  │           ├── 构造 SYSTEM_PROMPT（角色+规则+格式约束）
  │           ├── 发送用户咨询文本
  │           └── 解析返回 JSON → List[TaskPlan]
  │               ├── task_type: inventory_query/price_query/case_retrieval/price_calculation
  │               ├── tool_name: api_inventory/sql_price/doc_retrieve/calculator
  │               ├── parameters: {product_name, quantity...}
  │               ├── priority: 1/2/3
  │               └── dependencies: []/["inventory_query", "price_query"]
  │
  ├── Step 2: 工具调度执行
  │   └── ToolDispatcher.dispatch(tasks)
  │       ├── _group_by_priority()  # 按优先级分组
  │       ├── 循环按优先级执行
  │       │   ├── _check_dependencies() # 检查依赖是否满足
  │       │   └── _execute_parallel()   # 并行执行同优先级任务
  │       │       └── _execute_single() # 执行单个任务
  │       │           ├── 获取工具实例 self._tools[tool_name]
  │       │           ├── 合并参数 {**task.parameters, **context}
  │       │           ├── asyncio.wait_for(tool.execute(**params), timeout=30s)
  │       │           │   ├── tools/api_inventory_tool.py  # 库存查询
  │       │           │   ├── tools/sql_price_tool.py      # 价格查询
  │       │           │   ├── tools/doc_retrieve_tool.py   # 案例检索
  │       │           │   └── tools/calculator_tool.py     # 价格计算
  │       │           └── 记录 TaskLog（成功/失败/超时）
  │       └── 更新 context（供下游任务使用）
  │
  ├── Step 3: 构建响应数据
  │   └── SalesAgent._build_response(context)
  │       ├── 解析 inventory_query 结果 → InventoryInfo
  │       ├── 解析 price_query 结果 → PricingInfo
  │       ├── 解析 case_retrieval 结果 → List[CaseInfo]
  │       └── _generate_proposal()  # 调用大模型生成销售方案
  │           ├── 构造 PROPOSAL_PROMPT（库存+价格+案例+需求）
 │           ├── llm_adapter.chat()
  │           └── _parse_proposal() # 解析 JSON 为 ProposalInfo
  │
  ├── Step 4: 反思验真（可选，默认启用）
  │   └── ReflectionEngine.verify(response, context)
  │       ├── _verify_inventory()   # 比对库存数据真实性
  │       ├── _verify_pricing()     # 比对价格数据真实性
  │       ├── _verify_cases()       # 比对案例数据真实性
  │       └── 计算 overall_confidence（已验证字段 / 总字段）
  │           ├── confidence >= 0.8 → status = "success"
  │           └── confidence < 0.8  → status = "partial" + 警告
  │
  └── Step 5: 组装最终响应
      └── SalesResponse(
          status, message,
          inventory, pricing, cases, proposal,
          reflection_report, task_logs,
          customer_id, query, product_category, timestamp
      )
```

---

## 三、核心模块职责与交互关系

### 3.1 单例模式实例

| 单例 | 所在模块 | 创建时机 | 用途 |
|------|---------|---------|------|
| `settings` | `config/settings.py` | 模块导入时 | 全局配置读取（.env） |
| `_llm_adapter` | `core/llm_adapter.py` | 首次 `get_llm()` 调用 | 大模型调用封装 |
| `_agent` | `api/chat_route.py` | 首次请求时 | SalesAgent 实例复用 |

### 3.2 策略模式：LLM 适配器

```
BaseLLMAdapter (抽象基类)
  ├── OpenAIAdapter                # 云端模式（智谱/ OpenAI）
  │   └── llama_index.llms.openai.OpenAI
  └── QwenAdapter                  # 本地私有化模式（Ollama）
      └── llama_index.llms.ollama.Ollama

LLMAdapter (统一入口)
  └── 根据 settings.LLM_MODE 自动选择适配器
```

### 3.3 模板方法模式：工具基类

```
BaseTool (抽象基类)
  ├── execute(**kwargs)            # 子类必须实现
  ├── validate_params(**kwargs)    # 子类可覆盖
  ├── _get_parameters_schema()     # 子类必须实现
  ├── _handle_error()              # 通用错误处理
  └── _success_response()          # 通用成功响应

具体工具实现:
  ├── CalculatorTool               # 高精度数值计算
  ├── ApiInventoryTool             # 模拟 CRM 库存查询
  ├── SqlPriceTool                 # SQLite 历史价格查询
  └── DocRetrieveTool              # Chroma 向量案例检索
```

### 3.4 数据流向图

```
┌─────────────┐
│  客户咨询文本  │
└──────┬──────┘
       ▼
┌─────────────┐     ┌──────────────┐
│ TaskPlanner │────▶│  LLM Adapter │
│  (任务拆解)  │     │  (大模型推理) │
└──────┬──────┘     └──────────────┘
       ▼
┌─────────────────────────────────────────┐
│          ToolDispatcher (工具调度器)       │
│  ┌─────────┐ ┌─────────┐ ┌─────────────┐ │
│  │Priority1│→│Priority2│→│  Priority3  │ │
│  │库存查询  │ │价格查询  │ │ 价格计算     │ │
│  │(串行)   │ │(并行)   │ │ (依赖前置)   │ │
│  └────┬────┘ └────┬────┘ └──────┬──────┘ │
│       ▼           ▼             ▼        │
│  ┌─────────┐ ┌─────────┐ ┌─────────────┐ │
│  │ApiInventory│ │SqlPrice │ │  Calculator │ │
│  │  (Mock)  │ │(SQLite) │ │  (计算)      │ │
│  └─────────┘ └─────────┘ └─────────────┘ │
└─────────────────────────────────────────┘
       ▼
┌─────────────────────────────────────────┐
│         ReflectionEngine (反思验真)       │
│  ┌─────────┐ ┌─────────┐ ┌─────────────┐ │
│  │库存验真  │ │价格验真  │ │ 案例验真     │ │
│  │(字段比对)│ │(误差容忍)│ │ (相似度)     │ │
│  └─────────┘ └─────────┘ └─────────────┘ │
│              ↓ overall_confidence         │
└─────────────────────────────────────────┘
       ▼
┌─────────────────────────────────────────┐
│            SalesResponse (标准化输出)      │
│  {status, inventory, pricing, cases,    │
│   proposal, reflection_report, task_logs}│
└─────────────────────────────────────────┘
```

---

## 四、关键代码跳转路径

### 4.1 配置读取链

```
1. .env 文件
   ↓
2. config/settings.py::Settings (pydantic-settings 自动加载)
   ↓
3. 各模块通过 `from config.settings import settings` 使用
```

### 4.2 LLM 调用链

```
core/sales_agent.py:310
  └── self.llm.chat(messages, temperature=0.7)
      ↓
core/llm_adapter.py:425 (LLMAdapter.chat)
  └── self._adapter.chat(messages, temperature)
      ↓
core/llm_adapter.py:85 (OpenAIAdapter.chat)
  └── self.llm.achat(messages=chat_messages, temperature=temperature)
      ↓
llama_index.llms.openai.OpenAI.achat()
      ↓
openai.ChatCompletion.acreate() 或 httpx 异步请求
```

### 4.3 工具执行链

```
core/tool_dispatcher.py:207 (_execute_single)
  └── tool = self._tools.get(task.tool_name)
      ↓
  └── result = await asyncio.wait_for(tool.execute(**params), timeout=30)
      ↓
  根据 tool_name 跳转到对应工具:
    ├── "api_inventory" → tools/api_inventory_tool.py:65 (execute)
    ├── "sql_price"     → tools/sql_price_tool.py:48 (execute)
    ├── "doc_retrieve"  → tools/doc_retrieve_tool.py:48 (execute)
    └── "calculator"    → tools/calculator_tool.py:48 (execute)
```

### 4.4 数据库访问链

```
SQLite (价格数据):
  tools/sql_price_tool.py:48
    └── sqlite3.connect(settings.SQLITE_DB_PATH)
        └── 执行 SELECT 查询 deal_records / products 表

Chroma (向量案例):
  tools/doc_retrieve_tool.py:48
    └── chromadb.PersistentClient(settings.CHROMA_DB_PATH)
        └── collection.query(embeddings=..., n_results=3)
```

---

## 五、请求生命周期时序图

```
Client        FastAPI       SalesAgent    TaskPlanner   LLMAdapter   ToolDispatcher   Tools    Reflection
  │              │              │              │              │              │           │          │
  │─POST /api/chat/sales────▶│              │              │              │           │          │
  │              │─get_agent()─▶│              │              │              │           │          │
  │              │              │─process()───▶│              │              │           │          │
  │              │              │              │              │              │           │          │
  │              │              │              │─plan()──────▶│              │           │          │
  │              │              │              │              │─chat()──────▶│           │          │
  │              │              │              │◀─────────────│              │           │          │
  │              │              │              │              │              │           │          │
  │              │              │              │◀─List[TaskPlan]─────────────│           │          │
  │              │              │              │              │              │           │          │
  │              │              │─dispatch()─────────────────────────────────▶│           │          │
  │              │              │              │              │              │─Priority1  │          │
  │              │              │              │              │              │─Priority2──┼─并行执行──┤
  │              │              │              │              │              │─Priority3  │          │
  │              │              │              │              │              │◀───────────┼──────────┤
  │              │              │              │              │              │           │          │
  │              │              │              │◀─List[TaskLog]───────────────────────────│          │
  │              │              │              │              │              │           │          │
  │              │              │─_build_response()───────────────────────────────────────▶│          │
  │              │              │              │              │              │           │          │
  │              │              │              │              │─chat()──────▶│           │          │
  │              │              │              │              │◀─────────────│           │          │
  │              │              │              │              │              │           │          │
  │              │              │─verify()────────────────────────────────────────────────────────▶│
  │              │              │              │              │              │           │          │
  │              │              │◀─ReflectionReport────────────────────────────────────────────────│
  │              │              │              │              │              │           │          │
  │              │◀─SalesResponse─────────────────────────────────────────────────────────────────│
  │◀─JSON Response─────────────│              │              │              │           │          │
```

---

## 六、异常处理路径

```
工具执行异常:
  ToolDispatcher._execute_single()
    ├── asyncio.TimeoutError → TaskLog(status="failed", error="超时")
    └── Exception            → TaskLog(status="failed", error=str(e))

Agent 处理异常:
  SalesAgent.process()
    └── Exception → SalesResponse(status="error", message=str(e))

全局异常:
  api/chat_route.py:global_exception_handler()
    └── 返回 JSONResponse(status_code=500, detail=str(exc))
```

---

## 七、模块依赖关系图

```
main.py
  ├── config/settings.py
  ├── api/chat_route.py
  │   ├── core/sales_agent.py
  │   │   ├── core/llm_adapter.py
  │   │   ├── core/task_planner.py
  │   │   ├── core/tool_dispatcher.py
  │   │   ├── core/reflection_engine.py
  │   │   └── core/output_schema.py
  │   ├── tools/* (通过 __init__.py 导入)
 │   └── config/settings.py
  └── storage/__init__.py
      ├── storage/init_sql.py
      └── storage/init_vector_db.py
```

---

## 八、配置驱动的模块选择

| 配置项 | 取值 | 影响的模块 |
|--------|------|-----------|
| `LLM_MODE=openai` | openai | `core/llm_adapter.py` → OpenAIAdapter |
| `LLM_MODE=qwen` | qwen | `core/llm_adapter.py` → QwenAdapter |
| `USE_LOCAL_EMBEDDING=True` | True | `storage/init_vector_db.py` → 本地 BGE 模型 |
| `USE_LOCAL_EMBEDDING=False` | False | `storage/init_vector_db.py` → HuggingFace 在线模型 |
| `USE_MOCK_DATA=True` | True | `tools/api_inventory_tool.py` → 模拟数据 |
| `REFLECTION_ENABLED=True` | True | `core/sales_agent.py` → 启用反思验真 |
